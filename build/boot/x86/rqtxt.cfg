label rescue
	menu label ^Rescue mode
	kernel ${KERNEL}
	append initrd=${INITRD} rescue/enable=true fb=false -- quiet
label rescue_serial
	menu label Re^scue mode (serial console 115200)
	kernel ${KERNEL}
	append initrd=${INITRD} rescue/enable=true console=ttyS0,115200n8 -- quiet
